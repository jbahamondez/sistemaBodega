{
  "name": "sistema-bodega",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de control y trazabilidad de bodega - Chocolatería (Google Apps Script + Sheets)",
  "scripts": {
    "lint": "eslint src web",
    "test": "node scripts/run-tests.js",
    "check": "eslint src web && node scripts/syntax-check.js && node scripts/run-tests.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
